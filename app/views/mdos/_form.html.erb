

<%= form_for(mdo) do |f| %>
    <% if mdo.errors.any?  %>
        <div id="error_explanation">
          <h2><%= pluralize(mdo.errors.count, "error") %> prohibited this Donation from being saved:</h2>

          <ul>
            <% mdo.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
<table>
  <div class="field">
  <tr>
    <td>
      <%= f.label :Enter_Amount %>
      <%= f.text_field :amount  %>
    </td>
    <td>Or</td>
    <td>

      <input style="width: 10cm" type="range" min="1" max="100" value="0" oninput="showValue(this.value)" onchange="showValue(this.value)"  />
      <span id="range">0</span>
      <script type="text/javascript">
        function showValue(newValue)
        {
          document.getElementById("range").innerHTML=newValue;
        }

      </script>
<p id="myP" onmousedown="mouseDown()" onmouseup="mouseUp()"> You can click here mes] (5.1ms)
  Rendered /home/arunkumar/.rbenv/versions/2.2.4/lib/ruby/gems/2.2.0/gems/actionpack-5.0.0.1/lib/action_dispatch/middleware/templates/routes/_table.html.erb (1.2ms)
  Rendering /home/arunkumar/.rbenv/versions/2.2.4/lib/ruby/gems/2.2.0/gems/actionpack-5.0.0.1/lib/action_dispatch/middleware/templates/rescues/_request_and_response.html.erb
  Rendered /home/arunkumar/.rbenv/versions/2.2.4/lib/ruby/gems/2.2.0/gems/actionpack-5.0.0.1/lib/action_dispatch/middleware/templates/rescues/_request_and_response.html.erb (1.3ms)
  Rendered /home/arunkumar/.rbenv/versions/2.2.4/</p>
    </td>
  </tr>
  </div>

</table>
    <div class="field">
      <%= f.label :full_name %>
      <%= f.text_field :full_name %>
    </div>

    <div class="field">
      <%= f.label :company %>
      <%= f.text_field :company %>
    </div>

    <div class="field">
      <%= f.label :Email %>
      <%= f.text_field :email %>
    </div>

    <div class="field">
      <%= f.label :Telephone %>
      <%= f.text_field :telephone %>
    </div>
    <div class="field.payment_method">
      <table><tr>
        <td><%= f.label :card_payment  %></td><td><%= radio_button_tag('payment','card',class: "payment_method") %></td>
        <td><%= image_tag('credit-card.png', :style => "z-index:19; position: relative; ", size: '280x190') %></td>
        <td><%= f.label :paypal %></td><td><%= radio_button_tag('payment','paypal','checked = true',class: "payment_method")%></td>
        <td><%= image_tag('ppcom.svg', :style => "z-index:19; position: relative; ", size: '230x150') %></td>
      </tr> </table>
    </div>

    <script>
      $(document).ready(function(){

        $("#cardinfo").hide();

        $("input:radio[name='payment']").change(function(){

          if(this.value == 'card' && this.checked){
            <% @check_pay = "card" %>
            $("#cardinfo").show();
          }else{
            <% @check_pay = "paypal" %>
            $("#cardinfo").hide();
          }

        });

      });
    </script>
    <%= field_set_tag 'Enter Card details', id: 'cardinfo' do %>
        <%= f.fields_for :card do |c| %>
            <div class="field">
              <%= c.label :first_name %>
              <%= c.text_field :first_name %>
            </div>

            <div class="field">
              <%= c.label :last_name %>
              <%= c.text_field :last_name %>
            </div>

            <div class="field">
              <%= c.label :card_type %>
              <%= c.select :card_type, [["Visa", "visa"], ["MasterCard", "master"], ["Discover", "discover"], ["American Express", "american_express"]] %>
            </div>

            <div class="field">
              <%= c.label :card_number %>
              <%= c.text_field :card_number %>
            </div>

            <div class="field">
              <%= c.label :card_verification, "Card Verification Value (CVV)" %>
              <%= c.text_field :card_verification %>
            </div>
            <div class="field">
              <%= c.label :card_expires_on %>
              <%= c.date_select :card_expires_on, discard_day: true, start_year: Date.today.year, end_year: (Date.today.year+10), add_month_numbers: true %>
            </div>
        <% end %>
    <% end %>
    <div class="actions">
      <%= f.submit "Donate", class: "btn", style: "color: white;background: gray;", params: {check: :"paypal" }, data: {disable_with: "Submitting..."}%>
      <%= button_tag "Reset",class: "btn", style: "color: white;background: gray;", type: :reset %>
    </div>
<% end %>



<script>
  function mouseDown() {
    document.getElementById("myP").style.color = "red";
  }

  function mouseUp() {
    document.getElementById("myP").style.color = "green";
  }
</script>